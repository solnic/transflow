require 'spec_helper'

describe Transflow do
  it 'has a version number' do
    expect(Transflow::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
